{
  "name": "contracts",
  "version": "1.0.0",
  "scripts": {
    "build": "hardhat export-abi",
    "test": "hardhat test",
    "deploy:scheduler:local": "npx hardhat run --network local scripts/deployScheduler.ts",
    "deploy:scheduler:testnet": "npx hardhat run --network testnet scripts/deployScheduler.ts",
    "deploy:scheduler:mainnet": "npx hardhat run --network mainnet scripts/deployScheduler.ts",
    "deploy:factory:local": "npx hardhat run --network local scripts/deployFactory.ts",
    "deploy:factory:testnet": "npx hardhat run --network testnet scripts/deployFactory.ts",
    "deploy:factory::mainnet": "npx hardhat run --network mainnet scripts/deployFactory.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.2",
    "hardhat": "^2.17.1"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "4.9.3",
    "dotenv": "^16.3.1",
    "hardhat-abi-exporter": "^2.10.1"
  }
}
